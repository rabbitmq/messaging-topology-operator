apiVersion: rabbitmq.com.rabbitmq.com/v1beta1
kind: Vhost
metadata:
  labels:
    app.kubernetes.io/name: messaging-topology-operator
    app.kubernetes.io/managed-by: kustomize
  name: vhost-sample
spec:
  # TODO(user): Add fields here
